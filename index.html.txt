<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Special Page for Nikita 💖</title>
  <style>
    /* Body & Background */
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #ffe0f0, #fff0f5);
      height: 100vh;
      overflow: hidden;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    /* Card */
    .card {
      background: rgba(255, 255, 255, 0.9);
      border-radius: 25px;
      padding: 30px 40px;
      text-align: center;
      box-shadow: 0 8px 20px rgba(0,0,0,0.2);
      position: relative;
      z-index: 2;
      width: 90%;
      max-width: 500px;
    }

    /* Question */
    #question {
      font-size: 1.3rem;
      margin-bottom: 20px;
      min-height: 60px;
      transition: all 0.4s ease;
    }

    /* Buttons */
    .buttons {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-bottom: 20px;
    }

    button {
      background: #ffb6c1;
      border: none;
      padding: 10px 20px;
      border-radius: 20px;
      cursor: pointer;
      font-size: 1rem;
      color: #fff;
      transition: all 0.3s ease;
    }

    button:hover {
      background: #ff69b4;
      transform: scale(1.1);
    }

    #nextBtn {
      background: #ff69b4;
      margin-top: 10px;
      display: block;
      margin-left: auto;
      margin-right: auto;
    }

    /* Floating Teddies */
    .teddy {
      position: absolute;
      font-size: 2.5rem;
      animation: float 6s infinite ease-in-out;
      opacity: 0.8;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-15px); }
    }

    /* Cute confetti effect */
    .confetti {
      position: fixed;
      top: 0;
      left: 0;
      pointer-events: none;
      z-index: 5;
    }

  </style>
</head>
<body>

  <!-- Floating Teddies -->
  <div class="teddy" style="top: 5%; left: 10%;">🧸</div>
  <div class="teddy" style="top: 20%; left: 70%;">🧸</div>
  <div class="teddy" style="top: 50%; left: 15%;">🧸</div>
  <div class="teddy" style="top: 40%; left: 80%;">🧸</div>
  <div class="teddy" style="top: 75%; left: 30%;">🧸</div>
  <div class="teddy" style="top: 60%; left: 60%;">🧸</div>
  <div class="teddy" style="top: 10%; left: 50%;">🧸</div>
  <div class="teddy" style="top: 85%; left: 75%;">🧸</div>
  <div class="teddy" style="top: 30%; left: 40%;">🧸</div>

  <!-- Card with Questions -->
  <div class="card">
    <div id="question">Hey Nikita 💖, are you ready for a little surprise? 🧸</div>
    
    <div class="buttons" id="choiceButtons">
      <button onclick="answer('yes')">Yes 😊</button>
      <button onclick="answer('no')">No 😌</button>
    </div>
    
    <button id="nextBtn" onclick="nextQuestion()">Next ➡️</button>
  </div>

  <!-- Confetti Container -->
  <canvas class="confetti" id="confetti"></canvas>

  <script>
    // Questions and Answers
    const questions = [
      { question: "Do you know you have one of the prettiest smiles? 🌸", answer: "More prettiest than flowers 🌸😊" },
      { question: "Be honest… is the moon prettier or you? 🌙🎀", answer: "No comparison… many steps ahead of the moon 😏" },
      { question: "If I gave you a teddy today, would you keep it? 🧸", answerYes: "Thank you! 🧸💖", answerNo: "Rakh lo please! 😅" },
      { question: "Do you like when someone remembers tiny details about you? 🥺", answer: "I do 🥰" },
      { question: "Random question… have you saw the prettiest girl of world? ✨", answer: "To see her, just open your front camera 😏📸" },
      { question: "Would you smile if you knew this page was made only for you? 💖", answer: "Yes! It's only for you… after so many hours of coding and mistakes 😅😂" },
      { question: "Okay last one… should I make you smile again sometime? 🌷", answer: "Of course! Can't stop making you smile 😌💖" }
    ];

    let index = 0;

    // Show next question
    function nextQuestion() {
      if(index >= questions.length) return;

      const current = questions[index];
      const qDiv = document.getElementById("question");
      qDiv.innerText = current.question;

      const buttons = document.getElementById("choiceButtons");

      if(current.answerYes || current.answerNo){
        buttons.style.display = "flex";
      } else {
        buttons.style.display = "none";
        setTimeout(() => { qDiv.innerText = current.answer; }, 1000);
      }

      index++;
      if(index >= questions.length){
        document.getElementById("nextBtn").style.display = "none";
        launchConfetti();
      }
    }

    // Answer function for Yes/No
    function answer(option){
      const current = questions[index - 1];
      const qDiv = document.getElementById("question");

      if(option === "yes" && current.answerYes){
        qDiv.innerText = current.answerYes;
      } else if(option === "no" && current.answerNo){
        qDiv.innerText = current.answerNo;
      }

      document.getElementById("choiceButtons").style.display = "none";
    }

    // Confetti effect
    function launchConfetti() {
      const canvas = document.getElementById('confetti');
      const ctx = canvas.getContext('2d');
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;

      const confettiPieces = [];
      const colors = ["#FFB6C1","#FFC0CB","#FF69B4","#FF1493","#FFD1DC"];
      for(let i=0; i<150; i++){
        confettiPieces.push({
          x: Math.random()*canvas.width,
          y: Math.random()*canvas.height - canvas.height,
          r: Math.random()*6 + 4,
          d: Math.random()*20 + 10,
          color: colors[Math.floor(Math.random()*colors.length)],
          tilt: Math.floor(Math.random()*10)-10,
          tiltAngleIncrement: Math.random()*0.07+0.05,
          tiltAngle:0
        });
      }

      function draw(){
        ctx.clearRect(0,0,canvas.width,canvas.height);
        confettiPieces.forEach((p, i)=>{
          ctx.beginPath();
          ctx.lineWidth = p.r/2;
          ctx.strokeStyle = p.color;
          ctx.moveTo(p
